# TouchTerrain Core Dependencies
# Note: These should match pyproject.toml [project.dependencies]

Pillow>=10.4.0
google-api-python-client>=2.151.0
earthengine-api>=1.2.0
Flask>=3.0.3
oauth2client>=4.1.3
numpy>=1.17,<2.0  # CRITICAL: Must be <2.0 for GDAL compatibility
scipy>=1.14.1
kml2geojson>=5.1.0
geojson>=2.5
defusedxml>=0.7.1
six>=1.16.0
imageio>=2.36.0
k3d>=2.16.1
httplib2>=0.22.0
matplotlib>=3.9.2
shapely>=2.0
requests>=2.20
geopandas>=0.10
gunicorn>=20.0.4

# GDAL - Optional dependency (only for local DEM file processing)
# IMPORTANT: Install system libraries first, then install GDAL matching system version
# See DEVELOPMENT.md for detailed installation instructions
#
# Ubuntu/Debian:
#   sudo apt-get install gdal-bin libgdal-dev
#   GDAL_VERSION=$(gdal-config --version)
#   uv pip install setuptools wheel "numpy<2"
#   uv pip install --no-build-isolation "GDAL==$GDAL_VERSION"
#
# The line below is commented out because GDAL must match system version:
# gdal>=3.2.1,<=3.9.3
